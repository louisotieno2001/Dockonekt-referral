<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <style>
        body {
            margin: 0;
            padding: 0;
            background-image: url('bimageqi.avif');
            background-size: cover;
            background-position: center;
            min-height: 100vh;
        }

        .display {
            position: relative;
            top: -20px;
            /* Adjusted top value */
            padding: 15px;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }


        .logo {
            background-image: url('favicon.webp');
            background-position: center;
            background-size: cover;
            height: 60px;
            width: 60px;
            border-radius: 50%;
            margin: 2px;
            margin-left: 7px;
        }

        .bottom-nav {
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            background-color: #232323;
            padding: 10px;
            display: flex;
            justify-content: space-evenly;
            margin: 0;
            z-index: 999;
        }


        .buttons {
            display: flex;
            justify-content: left;
            gap: 20px;
            /* Adjusted padding for large screens */
            padding-left: 20px;
        }

        button {
            padding: 10px 20px;
            font-size: 1em;
            background-color: transparent;
            border: 2px solid #fff;
            color: #fff;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        button:hover {
            background-color: #fff;
            color: #000;
        }

        #title {
            padding: .1em;
            font-size: 100px;
            color: #fff;
        }

        #narration {
            padding-left: .3em;
            font-family: sans-serif;
        }

        .item {
            display: flex;
            align-items: center;
            color: #fff;
            cursor: pointer;
        }

        .material-symbols-outlined {
            margin-right: 10px;
        }

        .services {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 20px;
        }

        .card {
            flex: 1 1 calc(33.33% - 20px);
            margin: 10px;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: #fff;
            opacity: 70%;
        }

        .card h2 {
            font-size: 20px;
            color: #333;
            margin-bottom: 10px;
        }

        .card p {
            font-size: 16px;
            color: #666;
        }


        @media screen and (max-width: 600px) {
            #title {
                font-size: 50px;
                margin-bottom: -2px;
            }

            #narration {
                padding-left: .3em;
                display: none;
            }

            .buttons {
                /* Adjusted padding for small screens */
                padding-left: 3px;
                display: flex;
                flex-direction: column;
            }

            .services {
                margin-top: 0;
            }

            .card {
                flex: 1 1 calc(100% - 20px);
                margin: 10px 0;
            }

            .bottom-nav {
                position: relative;
                left: 0;
                bottom: 0;
                width: 100%;
                background-color: #232323;
                padding: 10px;
                display: flex;
                justify-content: space-evenly;
                margin: 0;
                z-index: 0;
            }
        }
    </style>
</head>

<body>
    <div class="display">
        <h1 id="title">Dockonekt</h1>
        <h3 id="narration">Welcome to DocKonekt. Looking to monitor patient referral real time? Use Dockonekt.
        </h3>
        <div class="services">
            <div class="card">
                <h2>Keep clean records</h2>
                <p>Maintaining pristine records is crucial for any organization. Utilize User
                    Dockonekt for seamless record-keeping and enhanced transparency.</p>
            </div>
            <div class="card">
                <h2>Fast referral services</h2>
                <p>In today's fast-paced environment, efficient referral services are indispensable. Harness
                    the power of Dockonekt for rapid and trustworthy referrals, ensuring quality and transparency
                    every step of the way.</p>
            </div>
            <div class="card">
                <h2>Secure platform</h2>
                <p>Security is paramount when it comes to managing patient data.
                    With Dockonekt, you can confidently monitor referrals, ensure transparency, and keep your records
                    secure,
                    all within a robust and reliable platform.</p>
            </div>
        </div>

        <div class="buttons">
            <button id="adminOfficerBtn">Hospital Admin</button>
            <button id="doctorOfficerBtn">Hospital Doctor</button>
            <button id="adminBtn">County department</button>
            <button id="subAdminBtn">Sub county department</button>
            <button id="subAdminBtnFull">Sub county health office</button>
            <button id="countyBtn">County health office</button>
            <button id="ministry">Health ministry office</button>
        </div>
    </div>
    <script>
        document.addEventListener('DOMContentLoaded', async function () {
            const adminOfficerBtn = document.getElementById('adminOfficerBtn');
            const doctorOfficerBtn = document.getElementById('doctorOfficerBtn');
            const countyAdminBtn = document.getElementById('adminBtn');
            const subCountyAdminBtn = document.getElementById('subAdminBtn');
            const subCountyAdminFull = document.getElementById('subAdminBtnFull');
            const countyBtn = document.getElementById('countyBtn');
            const ministry = document.getElementById('ministry');

            doctorOfficerBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                window.location.href = '/login';
            });

            adminOfficerBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                window.location.href = '/hospital/admin/login';
            });

            countyAdminBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                window.location.href = '/department/login';
            });

            ministry.addEventListener('click', async (e) => {
                e.preventDefault();
                window.location.href = '/health/ministry/login';
            });

            countyBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                window.location.href = '/county/login';
            });

            subCountyAdminBtn.addEventListener('click', async (e) => {
                e.preventDefault();
                window.location.href = '/sub/department/login';
            });

            subCountyAdminFull.addEventListener('click', async(e)=>{
                e.preventDefault();
                window.location.href = '/sub/county/department/register';
            })
        });
    </script>
</body>

</html>